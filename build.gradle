plugins {
	id 'convention.project.multi-root'
}

group = 'com.example'
version = '0.0.1'

ext {
	configDir = "${rootDir}/config"
	deploymentRepository = layout.buildDirectory.dir('deployment')
}

subprojects {
	group = rootProject.group
	version = rootProject.version
}